{
  "private": true,
  "name": "whitelist-manager",
  "version": "1.0.0",
  "description": "A whitelist manager for Minecraft servers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "stydxm",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
